# Papanie Webapp — Frontend

This directory contains a scaffolded React + TypeScript frontend using Vite and Tailwind.

Environment
- Create a .env file at the project root with:

VITE_API_URL=https://api.example.com

Scripts
- npm install
- npm run dev

Notes
- Auth endpoints are placeholders (/auth/login). Replace or wire to your backend as needed.
